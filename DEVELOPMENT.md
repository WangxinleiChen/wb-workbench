# 开发版验收说明 · v1.1.0-dev

> **公开仓库说明**：本文档提到的 `qa/`、`demo-private/`、`data/`、`examples/` 均为作者本机的私有验收材料与未发表实验图像，**不随仓库分发**。文中引用它们的地方仅供作者本机复核；下载者请用自己的图像验证。原 `STANDARD_RELEASE.md`（作者本机的恢复手册）也不在仓库内。

本目录是从 `standard-v1.0.0` 创建的 `development/background-v1.1.0` 独立 Git worktree。尚未合并或替换标准版。原有 `analysis.py` 未改；默认定量仍为模式 A。

## 启动、隔离和保存

双击本目录 `启动.command`，仅使用本目录 `.venv`，固定监听 `127.0.0.1:8766`。启动器检查已运行服务的 application 和 dataDir，发现其他服务占用时退出。服务禁止使用标准端口 8765、禁止 data 路径落到本开发目录之外。启动时不更新或安装依赖。

源码、`.venv` 中包文件、`data/`、`data/derived/`、`logs/`、`exports/` 均在本目录独立保存。开发 data 来自冻结时私有备份的普通文件复制，无指向标准版文件的软链接或硬链接。Git 元数据使用 worktree 的共享仓库；运行数据不共享。Python 标准库仍依赖 ENVIRONMENT.json 中记录的本机基础 Python，跨电脑恢复未验证。未新增第三方依赖。

- `data/images/`：原始导入文件和原有显示预览，永不覆盖旧原图。
- `data/derived/<key>/`：背景及带符号校正值 float64 NPY、仅供显示的 PNG、manifest。
- `data/state.json`、`data/audit.jsonl`：正式模式、候选预览、已应用版本及应用/修改/恢复记录。
- `logs/server-errors.log`：启动脚本的服务日志。
- `exports/`：请求导出的带时间戳副本，浏览器仍可下载。
- `qa/`、`demo-private/`：本轮本机私有验收材料，包含实验图像，已排除 Git；不作为可公开软件包。

备份请停止对应开发服务后复制完整 data；结果 ZIP 不能替代完整数据备份。标准版不承诺读取开发版新增字段，禁止把本目录 data 回写标准版。

## 唯一新增算法：稳健二次背景曲面

算法标识 `robust-quadratic-v1`，版本 1。在整块有效分析区域拟合
`B = c0 + c1*u + c2*v + c3*u² + c4*u*v + c5*v²`。
坐标 `u=2*(x-region.x)/(region.w-1)-1`，v 同理；只归一化坐标，**不归一化强度**。

从原始像素确定性均匀采样（包括区域边缘），最多 65,536 点。先最小二乘拟合，再按残差中位数和 `1.4826022185 × MAD` 对称剔除离群采样并重新拟合；上限 20 次，最终掩码重拟合。记录采样数、保留比例、迭代数、收敛状态、残差和系数，支持复算。允许重新接纳符合阈值的点，算法不是按组间差异优化。

唯一可调强度参数 `clipSigma` 是残差阈值的稳健尺度倍数，无量纲；默认 2.5，有效范围 1–6 的有限数。较小值排除更多偏离曲面的采样，但不保证更准确。参数对每张图整个分析区域统一生效，p 与 total 分别记录。区域每边至少 8 px，总面积最多 400 万 px，必须包含待测条带和足够多非条带背景；ROI 仍为原图像素坐标。

这是自有多项式背景模型。选择它是因为依赖现有 NumPy 即可实现，公式、参数、拟合结果可审查，适用于缓慢变化且背景占多数的区域。[ImageJ Subtract Background](https://imagej.net/ij/docs/guide/146-29.html) 使用 rolling ball / sliding paraboloid 等不同方法，本实现不声称与其等价。

### 强度、极性与内存

沿用未修改的读图函数：8/16 位灰度保留原始编码单位；RGB 仍使用已记录的灰度转换。所有拟合、差值和测量使用 float64，16 位不经过 8 位预览再计算。

- 亮条带：`S = I − B`；暗条带：`S = B − I`。
- 模式 B 净信号为条带 ROI 内 `sum(S)`，背景贡献为同一区域 `sum(B)`；保留原始积分、像素数和负值，不再扣旧背景框均值。
- 不截断非正净值；无效或未确认测量不发布比值。浮点舍入量级的正净值仍按既有原则视为需复核。
- 符号预览以灰度 127.5 对应零，两端按本次最大绝对残差映射；背景预览按 min/max 映射。区域外显示灰色 128，表示未处理。显示映射、缩放、截断均不进入测量。
- 背景曲面按行块求值；4M 区域每个 float64 数组约 32 MB，测量读取派生数组使用 mmap。拟合矩阵最多 65,536×6。原图解码仍沿用第一版整图读取，上限 40M，灰度 float64 本身可达 320 MB，RGB 转换/预览还需额外内存。这不是低内存流式大图解码器；内存紧张时缩小导入图和分析区域。

### 必须人工判断的局限

宽/密集条带、弥散信号、背景不占多数、突变背景或边缘趋势可能被曲面吸收而误扣。模型不能识别蛋白身份，不能恢复 JPEG/截图损失、饱和或未知处理历史。保留比例 100% 不证明条带与背景分开；未收敛和超出编码范围的拟合会提示复核，数值不会被悄悄裁剪。

WBTEST 的 total 图默认参数保留了全部采样，存在真实条带参与背景拟合的风险。因此本轮演示只验证软件流程，**不认可默认参数所得比值具有生物学定量有效性**；没有根据结果调参。

## 正式计算与预览互斥规则

1. 旧实验没有 background 字段时保持模式 A；读取不会迁移标准数据。
2. 输入 clipSigma 只是草稿；「预览」只新增候选及日志，不改正式模式、确认或结果。另一个参数的预览可以与已正式应用参数不同，界面会区分。
3. 「应用于定量」把当前候选设为正式模式 B，撤销该图全部确认，相关比值暂缺。任一指定对照不可用时整次实验相对指标暂缺。
4. 模式 B 的旧背景框只用于查看与恢复，不参与第二次扣除。条带重叠、越界和无效信号仍阻止正式比值。
5. 修改原图、极性或处理区域使候选和正式模型失效；不会悄悄回退 A。修改条带 ROI 撤销相关确认。仅切换显示或缩放不撤销确认。
6. 「恢复原方法」恢复 A 和保留的旧背景框，撤销相关确认；复核后同 ROI 可得到原方法数值。
7. 缓存 key 绑定原图 SHA-256、算法及版本、参数、极性、区域。派生数组校验哈希；目录名、manifest key 和正式 applied key 必须一致。每次改参数从原图重新计算，不累计扣除。

## 导出核对

保留原有列位置和模式 A 公式；追加当前模式、背景来源、贡献、版本、参数、原图 SHA、处理区域、缓存键及预览状态。模式 B XLSX 净值公式只做一次「背景贡献−原始积分」（亮条带反向），缓存和界面值一致。未确认和无效值继续留空。

ZIP 包含原图、状态快照、操作历史、旧标注预览，并额外包含候选/正式派生结果的 NPY、PNG、manifest 与使用索引。派生 PNG 明确标为显示结果，不是仪器原图。缺少应包含的派生文件时导出报错。

## 手动验收

1. 双击本目录启动入口，核对开发横幅及地址 8766；另一个标准版入口保持原位。
2. 新建或使用测试实验，记录模式 A 的 ROI、净信号、确认和结果。
3. 两图分别「预览」，查看「原图 / 背景估计 / 校正预览」；正式方法仍 A，确认和数值不变。
4. 明确应用一张图：应撤销该图确认，相应比值和整次对照相对指标失效。复核后确认，展开来源明细核对背景贡献。
5. 修改草稿参数后不能直接应用旧预览；重新预览时正式结果仍用旧参数，明确应用后再失效重确认。
6. 导出 CSV/XLSX/ZIP，查看正式模式、参数、原始积分、背景贡献和净值；ZIP 中核对原图 SHA 与派生 manifest。
7. 两图「恢复原方法」，重新确认；同一 ROI 的结果应回到第 2 步。重启后状态和结果保持。

## 自动测试

`.venv/bin/python3 -m unittest discover -s tests -v`

最终完整测试 61 PASS：原有 27、算法 16、模型流程 10、导出 8。算法覆盖恒定/渐变/噪声、8/16 位、暗/亮、弱/负信号、边界、非法参数及缓存错配。精确背景最大误差 <1e-7、积分 <1e-5；量化渐变背景 MAE <0.25、最大 <0.6 原始单位；确定性噪声背景误差 <0.3。完整数据和人工浏览器验收材料见 `qa/evidence/` 与总交付报告。

NOT RUN：跨电脑环境恢复、Excel 桌面重算、真实仪器线性/生物学定量验证。没有将合成测试视为生物学验证。
