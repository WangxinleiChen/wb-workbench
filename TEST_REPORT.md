# 第一版验证记录

日期：2026-09-20。环境：本机 macOS，Codex 已有 Python 3.12，NumPy、Pillow；浏览器为 Codex 内置浏览器。

## 自动测试

`python3 -m unittest discover -s tests -v`：**27 项通过**。

- 分析 14 项：已知像素积分、暗/亮条带、16 位灰度原值、透明/多页/浮点拒绝、16 位多通道 PNG 精度保护、选区边界与重叠、非正信号、浮点抵消、灰度端点语义、弱泳道与双峰候选、实际样例六泳道几何。
- 服务与导出 13 项：显式确认后才发布比值、修改选区撤销确认、缺失对照与多实验隔离、版本冲突、原图替换保留旧副本、重载复算与操作记录、XLSX 公式和缓存、CSV 字段和文本安全、ZIP 来源哈希、真实本机 HTTP 请求及跨站拒绝。
- 前端 JavaScript `node --check` 与启动器 `zsh -n` 通过。

## 浏览器实际操作

使用独立 `work/wb-build/qa-data` 测试目录完成：

1. 从空界面载入 WBTEST 两张图，检查六个样本与未确认状态。
2. 批量确认后比值发布；修改 Ctr1 坐标后，该样本确认撤销，其比值及所有相对对照值留空。
3. 实际拖动、缩放条带框，确认原图坐标和净值同步变化。
4. 刷新浏览器后坐标与确认状态恢复。
5. 新建第二个无对照实验，通过文件选择器导入 PHO.png，手动圈定目标区域并生成六个候选框。
6. 浏览器实际下载完整 ZIP；打开归档检查无损坏，XLSX 六条记录的缓存比值与 provenance.json 一致，未完成对照确认时相对值为空。
7. 界面已进行 1280 × 720 视觉检查，未观察到阻断操作的遮挡或浏览器控制台错误。

测试中发现并修复两项计算边界问题：16 位多通道 PNG 被图像库静默降位深；恒定 RGB 的浮点抵消残余可能被误当作极小正信号。

## 正式交付状态

正式数据位于本软件的 `data/`。仅预载一条 WBTEST 实验记录，6 个样本、双图候选框、**0 个样本已确认**。测试目录中的确认与编辑不进入正式记录。

已核对用户原图与随附副本逐字节一致（SHA-256 比对通过）。具体哈希值属于未发表的实验图像，不在公开仓库中列出；本机记录保留在私有备份的清单里。

## 未验证范围

测试验证软件数值和操作流程，不证明样例的仪器线性范围、曝光历史、跨膜上样/转膜一致性或生物学定量有效性。未在 Microsoft Excel 桌面应用中人工复算；XLSX 结构、公式引用和缓存使用 openpyxl 与应用计算结果交叉核对。大规模实验和所有相机/仪器的图像编码尚未逐一测试。
